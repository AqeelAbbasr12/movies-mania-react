export type Movie = {
  title: string,
  year: string,
  imdbID: string,
  poster: string,
}


export type WatchedMovies = {
  title: string,
  year: string,
  imdbID: string,
  poster: string,
  runtime: number,
  imdbRating: number,
  userRating: number
}